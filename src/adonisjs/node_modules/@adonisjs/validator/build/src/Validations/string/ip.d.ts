import { SyncValidation } from '@ioc:Adonis/Core/Validator';
/**
 * Validation signature for the "ip" regex. Non-string values are
 * ignored.
 */
export declare const ip: SyncValidation<{
    version?: 4 | 6;
}>;
