"use strict";
/*
 * @adonisjs/validator
 *
 * (c) Harminder Virk <virk@adonisjs.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeEmail = void 0;
const string_1 = require("@poppinss/utils/build/src/Helpers/string");
const normalizeEmail_1 = __importDefault(require("validator/lib/normalizeEmail"));
const helpers_1 = require("../../Validator/helpers");
const RULE_NAME = 'normalizeEmail';
/**
 * Normalize email address
 */
exports.normalizeEmail = {
    compile: (0, helpers_1.wrapCompile)(RULE_NAME, ['string'], (args) => {
        const options = Object.assign({}, args[0]);
        return {
            compiledOptions: Object.keys(options).reduce((result, key) => {
                result[(0, string_1.snakeCase)(key)] = options[key];
                return result;
            }, {}),
        };
    }),
    validate(value, compiledOptions, { mutate }) {
        /**
         * Ignore non string values
         */
        if (typeof value !== 'string') {
            return;
        }
        /**
         * Normalize email
         */
        mutate((0, normalizeEmail_1.default)(value, compiledOptions));
    },
};
