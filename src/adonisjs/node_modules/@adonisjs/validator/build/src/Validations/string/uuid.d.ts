import { UUIDVersion } from 'validator/lib/isUUID';
import { SyncValidation } from '@ioc:Adonis/Core/Validator';
/**
 * Validation signature for the "uuid" rule. Non-string values are
 * ignored.
 */
export declare const uuid: SyncValidation<{
    version?: UUIDVersion;
}>;
