import { FileTypeValidatorOptions } from '@nestjs/common/pipes/file/file-type.validator';
import { FileValidator } from '@nestjs/common/pipes/file/file-validator.interface';
import { MaxFileSizeValidatorOptions } from '@nestjs/common/pipes/file/max-file-size.validator';
import { ParseFileOptions } from '@nestjs/common/pipes/file/parse-file-options.interface';
import { ParseFilePipe } from '@nestjs/common/pipes/file/parse-file.pipe';
/**
 * @publicApi
 */
export declare class ParseFilePipeBuilder {
    private validators;
    addMaxSizeValidator(options: MaxFileSizeValidatorOptions): this;
    addFileTypeValidator(options: FileTypeValidatorOptions): this;
    addValidator(validator: FileValidator): this;
    build(additionalOptions?: Omit<ParseFileOptions, 'validators'>): ParseFilePipe;
}
