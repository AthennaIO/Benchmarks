import { LogItemInterface } from '.';
import { LogAttributes } from '../types';
declare class LogItem implements LogItemInterface {
    private attributes;
    constructor(params: {
        baseAttributes: LogAttributes;
        persistentAttributes: LogAttributes;
    });
    addAttributes(attributes: LogAttributes): LogItem;
    getAttributes(): LogAttributes;
    prepareForPrint(): void;
    removeEmptyKeys(attributes: LogAttributes): LogAttributes;
    setAttributes(attributes: LogAttributes): void;
}
export { LogItem };
//# sourceMappingURL=LogItem.d.ts.map