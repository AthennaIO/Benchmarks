/**
 * @athenna/artisan
 *
 * (c) João Lenon <lenon@athenna.io>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
import { Command, type Help } from 'commander';
export declare class Formatter {
    static builder(cmd: Command, help: Help): Formatter;
    private cmd;
    private help;
    private termWidth;
    private itemSeparatorWidth;
    private helpWidth;
    private indent;
    private moveOptions;
    private matchRequiredArgs;
    private matchOptionsAndOptionalArgs;
    private sections;
    constructor(cmd: Command, help: Help);
    setUsage(): this;
    setDescription(): this;
    setOptions(): this;
    setCommands(): this;
    setArguments(): this;
    setGlobalOptions(): this;
    getOutput(): string;
    command(cmd: string, desc: string): string;
    option(term: string, desc: string): string;
    item(term: string, desc: string): string;
    list(values: string[]): string;
    term(term: string, options?: {
        greenColor?: boolean;
        padEnd?: boolean;
        argsColor?: boolean;
    }): string;
}
