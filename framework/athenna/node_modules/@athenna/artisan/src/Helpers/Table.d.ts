/**
 * @athenna/artisan
 *
 * (c) João Lenon <lenon@athenna.io>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
import CliTable from 'cli-table3';
import { TableRow } from '#src/Types/TableRow';
export declare class Table {
    /**
     * The state of the table. This property will
     * have the head and rows of your table.
     */
    state: {
        head: any[];
        rows: any[];
    };
    /**
     * Define table headers.
     */
    head(...headers: string[]): Table;
    /**
     * Define table rows.
     */
    row(...rows: TableRow[]): Table;
    /**
     * Create the table and return as string.
     */
    toString(opts?: CliTable.TableConstructorOptions): string;
    /**
     * Create the table string and render it.
     */
    render(opts?: CliTable.TableConstructorOptions): void;
    /**
     * Simple vanilla logger implementation to work the same way
     * of Artisan logger.
     */
    private log;
}
