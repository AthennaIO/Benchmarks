/**
 * @athenna/artisan
 *
 * (c) João Lenon <lenon@athenna.io>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
import 'reflect-metadata';
import type { ArgumentOptions, OptionOptions } from '#src/types';
export type OptionsMeta = {
    key: string;
    signatureName: string;
} & OptionOptions;
export type ArgumentMeta = {
    key: string;
    signatureName: string;
} & ArgumentOptions;
export declare class Annotation {
    static setOption(target: any, key: string, options: OptionOptions): typeof Annotation;
    static setArgument(target: any, key: string, argument: ArgumentOptions): typeof Annotation;
    static getOptions(target: any): OptionsMeta[];
    static getArguments(target: any): ArgumentMeta[];
}
