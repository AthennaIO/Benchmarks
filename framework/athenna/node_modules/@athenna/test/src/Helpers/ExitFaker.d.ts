/**
 * @athenna/test
 *
 * (c) João Lenon <lenon@athenna.io>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
import { SinonSpy } from 'sinon';
export declare class ExitFaker {
    static original: (code?: number) => never;
    static faker: SinonSpy<[code?: number], never>;
    /**
     * Fake the process.exit method.
     */
    static fake(): typeof ExitFaker;
    /**
     * Release the faker process.exit method.
     */
    static release(): typeof ExitFaker;
}
