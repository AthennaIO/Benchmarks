/**
 * @athenna/core
 *
 * (c) João Lenon <lenon@athenna.io>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
import { ServerImpl } from '@athenna/http';
import { Ignite, type HttpOptions, type IgniteOptions } from '@athenna/core';
export declare class BaseHttpTest {
    ignite: Ignite;
    httpServer: ServerImpl;
    httpOptions: HttpOptions;
    igniteOptions: IgniteOptions;
    baseBeforeAll(): Promise<void>;
    baseAfterAll(): Promise<void>;
    private getHttpOptions;
    private getIgniteOptions;
}
